#include "Position.h"

inline Position newPosition(unsigned int x, unsigned int y)
{
    //inutile de commenter quoi que ce soit ici...
    Position res;
    res.x = x;
    res.y = y;
    return res;
}
